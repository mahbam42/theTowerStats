"""Placeholder for the authoritative enforcement spec tests.

This file captures the enforcement test intent for the layered game data
architecture. The required apps (`definitions` and `gamedata`) are not yet
present in the current codebase; when they are introduced, these tests should be
implemented to execute the rules described in the specification.
"""

import pytest

pytest.skip(
    "Enforcement spec requires definitions/gamedata apps that are not available yet.",
    allow_module_level=True,
)
